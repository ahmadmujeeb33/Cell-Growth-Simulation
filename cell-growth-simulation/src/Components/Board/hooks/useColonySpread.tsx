

import React, { useEffect, useState } from 'react';



export const useColonySpread = (spreading: boolean, grid:boolean[][]) => {

    // const existingBacteriaCells = {

    // const getExistingColony = () => {
        
    //     grid.map((row, rIdx) => 
    //         row.map((cell, cIdx) => (rIdx === rowIndex && cIdx === colIndex ? !cell : cell))
    //     )


    // }

    const updateColony = () => {

        // getExistingColony()
    }

    useEffect(() =>{

        updateColony()

    }, [spreading])
}

